# python and sudo are required for closure linter:
# `sudo python setup.py install`
language: python
sudo: required
script:
  - ./build/jslint.sh
  - ./build/jsmin.sh
before_install:
  - git submodule update --init --recursive
  - pwd
  - ln -s glize/src src/glize
  - ls -la src/glize
